<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Test3.DataVirtualization.HeavyListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Test3.DataVirtualization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="ProductList" ItemsSource="{x:Bind Products}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Product">
                    <Border
                        Margin="4"
                        Padding="8"
                        BorderBrush="Gray"
                        BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--<Path
                                Width="20"
                                Height="20"
                                Data="M10,1 L12,7 L18,7 L13,10 L15,16 L10,12 L5,16 L7,10 L2,7 L8,7 Z"
                                Fill="Gold" />-->
                            <!--<Image
                            Width="20"
                            Height="20"
                            Source="/Assets/star.svg" />-->
                            <Image
                                Width="120"
                                Height="120"
                                Margin="8"
                                Source="{x:Bind ImagePath}" />

                            <StackPanel Grid.Column="1" Margin="12,0">
                                <TextBlock
                                    FontSize="22"
                                    FontWeight="Bold"
                                    Text="{x:Bind Name}" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="SemiBold" Text="Price: " />
                                    <TextBlock Foreground="Green" Text="{x:Bind Price, Mode=OneWay}" />
                                </StackPanel>
                                <TextBlock
                                    Width="300"
                                    Text="{x:Bind Description}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
