<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Test3.XAMLLayout.ReduceLayoutStructure.RelativePanelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Test3.XAMLLayout.ReduceLayoutStructure"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Left: original RelativePanel UI -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <RelativePanel>
                <TextBlock x:Name="Options" Text="Options:" />
                <CheckBox
                    x:Name="PowerUser"
                    Content="Power User"
                    RelativePanel.Below="Options" />
                <CheckBox
                    Margin="20,0,0,0"
                    Content="Admin"
                    RelativePanel.Below="Options"
                    RelativePanel.RightOf="PowerUser" />
                <TextBlock
                    x:Name="BasicInformation"
                    RelativePanel.Below="PowerUser"
                    Text="Basic information:" />
                <TextBlock RelativePanel.AlignVerticalCenterWith="NameBox" Text="Name:" />
                <TextBox
                    x:Name="NameBox"
                    Width="200"
                    Margin="75,0,0,0"
                    RelativePanel.Below="BasicInformation" />
                <TextBlock RelativePanel.AlignVerticalCenterWith="EmailBox" Text="Email:" />
                <TextBox
                    x:Name="EmailBox"
                    Width="200"
                    Margin="75,0,0,0"
                    RelativePanel.Below="NameBox" />
                <TextBlock RelativePanel.AlignVerticalCenterWith="PasswordBox" Text="Password:" />
                <TextBox
                    x:Name="PasswordBox"
                    Width="200"
                    Margin="75,0,0,0"
                    RelativePanel.Below="EmailBox" />
                <Button Content="Save" RelativePanel.Below="PasswordBox" />
            </RelativePanel>
        </ScrollViewer>

        <!-- Right: code example with syntax coloring and copy button -->
        <Border Grid.Column="1" Margin="12,0,0,0" Padding="8" BorderBrush="Gray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Stretch" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="XAML Example" />
                    <Button x:Name="CopyButton" Margin="8,0,0,0" HorizontalAlignment="Right" Click="CopyButton_Click" Content="Copy" />
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <RichTextBlock x:Name="CodeBlock" FontFamily="Consolas" FontSize="12" IsTextSelectionEnabled="True" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
